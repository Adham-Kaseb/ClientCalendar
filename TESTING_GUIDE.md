# 🧪 دليل اختبار وتقييم قدرات تقويم مشروع تايم فالي
#(Interactive Calendar Capability & Feature Testing Guide)

هذا المستند يوفر دليلاً شتملاً وممنهجاً لاختبار كافة خصائص وميزات **التقويم التفاعلي لمشروع تايم فالي (Project Timevalley)** للتأكد من عمل كافة الوظائف، الصلاحيات، الربط اللحظي بقاعدة البيانات، والأنيميشن التفاعلي.

---

## 📋 نظرة عامة على معلومات الدخول والاختبار

| الحساب | اسم المستخدم (Username) | كلمة المرور (Password) | دور الحساب (Role) | الصلاحيات والوظائف |
| :--- | :--- | :--- | :--- | :--- |
| **د. وائل (العميل)** | `dr.wael` | `timevalley` | `client` | الاطلاع، كتابة التعليقات والملاحظات، اعتماد التقارير، إرسال التلخيص الإيميل |
| **أدهم كاسب (المُنفّذ)** | `adham` | `pass` | `executor` | تسجيل وتحديث الإنجازات اليومية، إضافة التسليمات، تحديد نسبة الإنجاز والردود |
| **وضع المطوّر (السرّي)** | `ssss` | — | — | كشف وإظهار أزرار الدخول السريع بنقرة واحدة |

---

## 🔑 المرحلة 1: اختبار بوابات الدخول والوضع السري (Authentication Gate)

### 🧪 اختبار 1.1: واجهة تسجيل الدخول الرسمية
- **الخطوات**:
  1. افتح التطبيق عبر الرابط `http://localhost:9999`.
  2. تأكد من ظهور شاشة تسجيل الدخول الرسمية مع شعار `مشروع تايم فالي`.
- **النتيجة المتوقعة**: الواجهة نظيفة ومحمية، ولا تظهر أي أزرار اختبار عامة بشكل افتراضي.

### 🧪 اختبار 1.2: اختبار البيانات الخاطئة
- **الخطوات**:
  1. أدخل اسم مستخدم: `test_user` وكلمة مرور: `123456`.
  2. انقر على **دخول التقويم التفاعلي**.
- **النتيجة المتوقعة**: ظهور تنبيه أحمر: `اسم المستخدم أو كلمة المرور غير صحيحة`.

### 🧪 اختبار 1.3: كشف خيارات الدخول السريع عبر الرمز السرّي (`ssss`)
- **الخطوات**:
  1. في أسفل كارت الدخول، انقر على *"تفعيل خيارات الدخول السريع للاختبار"*.
  2. أدخل الرمز السرّي: **`ssss`**.
- **النتيجة المتوقعة**: ظهور شريط أخضر (`تم تفعيل الدخول السريع للاختبار بنجاح!`) وتظهر أزرار الدخول بنقرة واحدة لـ **د. وائل** و **أدهم** بأسلوب أنيميشن سلس.

---

## 👥 المرحلة 2: اختبار إدارة الصلاحيات والأدوار (Role-Based Access Control)

### 🧪 اختبار 2.1: الدخول بحساب أدهم كاسب (المُنفّذ)
- **الخطوات**:
  1. سجل الدخول بحساب أدهم (`adham` / `pass`).
  2. شاهد شاشة التحميل الترحيبية لمدة 2 ثانية (`مرحباً بك، أدهم كاسب`) يليها الهيكل العظمي (0.5 ثانية).
- **النتيجة المتوقعة**:
  - الهيدر يعرض شارة: `أدهم كاسب (مُنفّذ المشروع)` بخلفية خضراء.
  - ظهور زر: `+ تسجيل إنجاز اليوم` في الهيدر.
  - خلايا التقويم الفارغة تُظهر زر: `+ إضافة`.
  - اختفاء شريط التبديل اليدوي بين الأدوار تماماً.

### 🧪 اختبار 2.2: الدخول بحساب د. وائل (العميل الرئيسي)
- **الخطوات**:
  1. انقر على أيقونة تسجيل الخروج في الهيدر للعودة لشاشة الدخول.
  2. سجل الدخول بحساب د. وائل (`dr.wael` / `timevalley`).
- **النتيجة المتوقعة**:
  - الهيدر يعرض شارة: `د. وائل (العميل الرئيسي)` مع التاج الذهبي.
  - ظهور زر: `إرسال إشعار تلخيصي` (Resend Email Alert).
  - اختفاء أزرار `+ تسجيل إنجاز اليوم` و `+ إضافة` تماماً ومنع التعديل المباشر.

---

## 📅 المرحلة 3: اختبار التقويم التفاعلي والفلترة (Calendar Grid & Custom Select)

### 🧪 اختبار 3.1: التنقل بين الأشهُر وحركة السلايد
- **الخطوات**:
  1. انقر زر `ChevronRight` (الشهر السابق) -> تنقل الشبكة لشهر يوليو 2026.
  2. انقر زر `اليوم` -> إعادة ضبط الشبكة لشهر أغسطس 2026.
  3. انقر زر `ChevronLeft` (الشهر التالي) -> تنقل الشبكة لشهر سبتمبر 2026.
- **النتيجة المتوقعة**: انزلاق التواريخ بسلاسة مع أنيميشن Framer Motion.

### 🧪 اختبار 3.2: اختبار قائمة الفلترة المخصصة (`CustomSelect.tsx`)
- **الخطوات**:
  1. انقر على قائمة الفلترة (`كافة الإنجازات`).
  2. اختر `المكتملة (100%)`.
  3. اختر `قيد التنفيذ`.
  4. اختر `محتمل التأخير`.
- **النتيجة المتوقعة**: فتح قائمة مخصصة بوعاء زجاجي ونقاط ملونة وتحديث بطاقات الإنجاز المعروضة.

### 🧪 اختبار 3.3: التبديل بين عرض التقويم الشهري والقائمة اليومية
- **الخطوات**:
  1. انقر على `قائمة يومية` (Daily List View).
  2. لاحظ انزلاق مؤشر التبويب في الخلفية.
  3. انقر على `تقويم شهري` (Month Grid View).
- **النتيجة المتوقعة**: انزلاق المؤشر بسلاسة والتبديل الفوري بين العرضين.

---

## 📝 المرحلة 4: تسجيل الإنجازات والربط اللحظي (Daily Log & Supabase Sync)

### 🧪 اختبار 4.1: إضافة إنجاز يومي جديد (أدهم)
- **الخطوات**:
  1. سجل الدخول بحساب **أدهم** (`adham` / `pass`).
  2. انقر على `+ تسجيل إنجاز اليوم` (أو زر `+ إضافة` على أي يوم فارغ).
  3. قم بتعبئة النموذج:
     - **العنوان**: `تأهيل نظام التنبيهات وإدارة الأدوار`
     - **الملخص**: `تم تطوير صفحة الدخول وتقسيم الصلاحيات بين د. وائل وأدهم`
     - **ساعات العمل**: `7.5`
     - **نسبة الإنجاز**: `100%`
     - **الحالة**: `مكتمل 100% (Completed)`
     - **التسليمات**: أضف تسليمتين (`ربط Supabase Realtime` و `ضبط أنيميشن Framer Motion`).
     - **ملاحظات**: `روابط Figma والتوثيق جاهز للاستعراض`
  4. انقر على **حفظ ونشر التقرير اللحظي**.
- **النتيجة المتوقعة**:
  - إغلاق النافذة بأنيميشن زمبركي.
  - ظهور توست الإشعار: `تم حفظ الإنجاز بنجاح ونشر الإشعار اللحظي لـ د. وائل`.
  - ظهور بطاقة الإنجاز فوراً على يوم 11 أغسطس 2026.
  - حفظ البيانات فورياً في جدول `daily_logs` على PostgreSQL في Supabase.

---

## 💬 المرحلة 5: اعتماد التقارير وتحديثات المناقشة (Dr. Wael Approval & Discussions)

### 🧪 اختبار 5.1: استعراض تفاصيل الإنجاز والتسليمات
- **الخطوات**:
  1. انقر على أي بطاقة إنجاز في التقويم.
  2. تفتح نافذة التفاصيل (`DayDetailsModal`).
  3. استعرض الملخص التنفيذي، ساعات العمل، وقائمة التسليمات المكتملة.
- **النتيجة المتوقعة**: عرض التفاصيل بتنسيق واضح وأنيق.

### 🧪 اختبار 5.2: اعتماد التقرير من د. وائل
- **الخطوات**:
  1. سجل الدخول بحساب **د. وائل** (`dr.wael` / `timevalley`).
  2. افتح أي تقرير إنجاز يومي.
  3. انقر على زر **اعتماد التقرير د. وائل**.
- **النتيجة المتوقعة**:
  - ظهور توست: `تم اعتماد تقرير الإنجاز اليومي بنجاح من د. وائل!`.
  - تحديث حالة الإنجاز لـ `مكتمل 100%` وحفظه في Supabase.
  - إرسال إشعار لحظي لأدهم يفيد باعتماد التقرير.

### 🧪 اختبار 5.3: إضافة تعليق وملاحظة في حلقة المناقشة (`CommentSection.tsx`)
- **الخطوات**:
  1. داخل نافذة التفاصيل، انتقل لتبويب **"تعليقات وملاحظات د. وائل"**.
  2. اكتب تعليقاً: `ممتاز جداً يا أدهم، الواجهات متناسقة والربط فوري.`
  3. انقر على **إرسال**.
- **النتيجة المتوقعة**:
  - إدراج التعليق فوراً برتبة `العميل` ورمز التاج لـ د. وائل.
  - حفظ التعليق في جدول `comments` في Supabase.

---

## 🔔 المرحلة 6: مركز التنبيهات وإرسال التلخيص (Notifications & Resend Digest)

### 🧪 اختبار 6.1: درج التنبيهات اللحظية (`NotificationCenter.tsx`)
- **الخطوات**:
  1. انقر على أيقونة الجرس في الهيدر.
  2. لاحظ انزلاق النافذة الزجاجية مع رأس السهم العلوي وعداد التنبيهات الجديدة.
  3. انقر على أي إشعار غير مقروء.
- **النتيجة المتوقعة**: تحول حالة الإشعار إلى `مقروء` مع شارة الصح المزدوج الأخضر.

### 🧪 اختبار 6.2: إرسال إشعار تلخيصي عبر الإيميل
- **الخطوات**:
  1. بحساب د. وائل، انقر على زر **إرسال إشعار تلخيصي** في الهيدر.
- **النتيجة المتوقعة**: ظهور توست: `تم إرسال إيميل تلخيص الإنجاز اليومي عبر Resend إلى د. وائل (wael@timevalley.com)` وإدراج تنبيه جديد.

---

## 🎯 ملخص نتائج الاختبار والجاهزية

- [x] **بوابة الدخول والوضع السرّي (`ssss`)**: مكتملة ومفحوصة 100%.
- [x] **أنيميشن التحميل (Splash 2s + Skeleton 0.5s)**: يعمل بسلاسة تامة.
- [x] **فصل الصلاحيات والأدوار (RBAC)**: محمي ومربوط بحساب المستخدم المسجل.
- [x] **التقويم الشهري والفلترة المخصصة (`CustomSelect`)**: يعمل بأنيميشن سلس.
- [x] **قاعدة البيانات والربط اللحظي (Supabase Realtime)**: متصل ويعمل لحظياً على البورت `9999`.
